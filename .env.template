# WireGuard Monitor Email Configuration
# Replace these values with your actual SMTP settings

# SMTP Server Configuration
SMTP_SERVER=localhost
SMTP_PORT=25

# Email Addresses
SMTP_EMAIL=sender@yourdomain.com
SMTP_PASSWORD=your_password  # Optional, only if your SMTP server requires authentication

# Recipient for security alerts
ALERT_EMAIL=recipient@domain.com

# WireGuard Configuration
LOG_FILE_PATH=/var/log/wireguard/wg0.log  # Path to WireGuard log file
